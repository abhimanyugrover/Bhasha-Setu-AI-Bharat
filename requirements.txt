# Bhasha Setu — Python Dependencies
# Install with: pip install -r requirements.txt
#
# System dependency (must be installed separately):
#   Ubuntu/Debian: sudo apt install ffmpeg
#   macOS:         brew install ffmpeg
#   Windows:       https://ffmpeg.org/download.html (add to PATH)

# ── Core AWS ─────────────────────────────────────────────────────────
boto3>=1.34.0
botocore>=1.34.0

# ── Translation ──────────────────────────────────────────────────────
deep-translator>=1.11.4

# ── TTS Engines ──────────────────────────────────────────────────────
edge-tts>=6.1.10
gTTS>=2.5.1

# ── Streamlit UI ─────────────────────────────────────────────────────
streamlit>=1.35.0

# ── Optional: LLM Summarizer backends (uncomment to enable) ──────────
# groq>=0.9.0                   # Groq — llama-3.1-8b, very fast, free tier
# google-generativeai>=0.7.0    # Google Gemini Flash — free tier
